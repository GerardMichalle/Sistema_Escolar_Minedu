# Orquestación prevista para el sistema completo.
# El frontend ya puede levantarse; backend y db se activarán
# cuando esos módulos estén desarrollados.
services:
  frontend:
    build:
      context: ../frontend
      dockerfile: ../docker/Dockerfile
    ports:
      - "5173:5173"

  # backend:
  #   build: ../backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/sistema_colegios
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: sistema_colegios
  #     POSTGRES_USER: colegio
  #     POSTGRES_PASSWORD: cambiar_en_produccion
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   pgdata:
